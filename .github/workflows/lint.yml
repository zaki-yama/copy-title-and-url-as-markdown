name: Lint

on:
  push:
    branches:
      - master
  pull_request:

jobs:
  build:
    name: Node.js latest
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6
      - uses: pnpm/action-setup@v5
      - name: Setup node
        uses: actions/setup-node@v6
        with:
          node-version: latest
          cache: 'pnpm'
      - name: test
        run: |
          pnpm install
          pnpm fmt:check
          pnpm lint
          pnpm typecheck
        env:
          CI: true
